As of July 3, 2026, the expensive files are calculate_T.py and calculate_tau.py. Both use the function calculate\_\<T or tau\>\_edges(), which allow only for specifying x and y ranges explicitly. The functions calculate\_\<T or tau\>\_chunks() will implement chunking completely, but currently implement older versions of calculate\_\<T or tau\>\_edges() (with the beginnings of chunking implemented) and should not be used. 
